type User {
  id: ID
  name: String
  lastName: String
  email: String
}

input AuthInput {
  email: String!
  password: String!
}

type Token {
  token: String!
}

type Query {
  getUser: [User]
  authUser(input: AuthInput!): Token
  authorization(token: String!): User
}
