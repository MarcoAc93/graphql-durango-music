type User {
  id: ID
  name: String
  lastName: String
  email: String
  password: String
}

input AuthInput {
  email: String!
  password: String!
}

type Token {
  token: String!
}

type Query {
  getUser: [User]
  authUser(input: AuthInput!): Token
}
